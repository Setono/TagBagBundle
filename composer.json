{
    "name": "setono/tag-bag-bundle",
    "description": "A Symfony bundle that creates a session bag called tag which can be used to inject i.e. script tags on your website",
    "type": "symfony-bundle",
    "license": "MIT",
    "authors": [
        {
            "name": "Joachim Løvgaard",
            "email": "joachim@loevgaard.dk"
        }
    ],
    "require": {
        "php": "^7.1.3",
        "symfony/config": "^3.4|^4.1",
        "symfony/dependency-injection": "^3.4|^4.1",
        "symfony/framework-bundle": "^3.4|^4.1",
        "symfony/http-foundation": "^3.4|^4.1"
    },
    "require-dev": {
        "phpstan/phpstan": "^0.10.7",
        "roave/security-advisories": "dev-master",
        "symplify/easy-coding-standard": "^5.2"
    },
    "prefer-stable": true,
    "autoload": {
        "psr-4": {
            "Setono\\TagBagBundle\\": ""
        },
        "exclude-from-classmap": [ "/Tests/" ]
    },
    "extra": {
        "branch-alias": {
            "dev-master": "1.0-dev"
        }
    },
    "config": {
        "sort-packages": true
    },
    "scripts": {
        "analyse": "./vendor/bin/phpstan analyse -c phpstan.neon -l max",
        "check-style": "./vendor/bin/ecs check DependencyInjection/ HttpFoundation/",
        "fix-style": "./vendor/bin/ecs check --fix DependencyInjection/ HttpFoundation/"
    }
}
